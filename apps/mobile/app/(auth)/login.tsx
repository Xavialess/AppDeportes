import { View, Text } from 'react-native';

// Placeholder — to be implemented
export default function LoginScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Login — por implementar</Text>
    </View>
  );
}
